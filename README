Every Folder is a different project, contain only files of code specific to the project without any boilerplate. 
To be used as a source of reference

"Copy of htaccess" would be need to changed to .htaccess
